image: adoptopenjdk/openjdk14:jdk-14.0.2_12

stages:
  - build
  - validate
  - test

variables:
  # Disable the Gradle daemon to ensure isolated runs of the CI pipeline.
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

service-admin-build:
  stage: build
  script:
    ./gradlew --build-cache :services:core:assemble
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - build/
      - .gradle/

service-admin-checkstyle:
  stage: validate
  script: ./gradlew :services:core:checkstyleMain :services:core:checkstyleTest
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - build/
      - .gradle/
  allow_failure: true

service-admin-test:
  stage: test
  script:
    - ./gradlew :services:core:test :services:core:jacocoTestReport
    - cat services/core/build/reports/jacoco/html/index.html | grep -Po "Total.*?([0-9]{1,3})%"
  coverage: /([0-9]{1,3})%/
